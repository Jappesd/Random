[project]
name = "weather-app"
version = "0.1.0"
description = "Simple terminal weather app"
requires-python = ">=3.10"
dependencies = ["requests"]

[project.scripts]
weather = "weather_app.main:main"